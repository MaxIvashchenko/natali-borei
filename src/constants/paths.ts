import { IPaths } from '@src/types';

const paths: IPaths = {
  main: '/',
  shop: '/shop',
  about: '/about',
  favorites: '/favorites',
  admin: '/admin',
  addItem: '/admin/add',
};

export default paths;
